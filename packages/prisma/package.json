{
  "name": "@cs-magic/prisma",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/generated/zod/index.js",
  "exports": {
    ".": "./dist/generated/zod/index.js",
    "./generated/zod": "./dist/generated/zod/index.js",
    "./*": "./*"
  },
  "scripts": {
    "build": "tspc -b",
    "prisma": "npx dotenv -e ../../.env -- prisma",
    "generate": "yarn prisma generate && tspc -b",
    "migrate": "yarn prisma migrate",
    "migrate:dev": "yarn migrate dev",
    "migrate:deploy": "yarn migrate deploy"
  },
  "dependencies": {
    "prisma": "^5"
  }
}
